# railway.toml

[services.backend]
# Path to your Node.js backend code
path = "WebApp/Backend"

# Build & start commands
builder = "nodejs"
build = "npm install"
start = "npm start"

# Railway deploy config
[services.backend.deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
